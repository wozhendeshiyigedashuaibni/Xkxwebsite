# DEPRECATED - 已废弃

此目录 (`server/`) 已废弃，不再用于生产环境。

## 生产环境使用

生产环境使用 Vercel Serverless Functions，代码位于 `api/` 目录。

## 本目录保留原因

- 仅供本地开发参考
- 部分逻辑可能需要迁移到 `api/` 或 `lib/`

## 请勿

- 不要在生产环境引用此目录中的任何文件
- 不要修改此目录中的代码（除非明确需要迁移）

## 迁移完成后可删除

当确认所有功能都已迁移到 `api/` 和 `lib/` 后，可以安全删除此目录。
